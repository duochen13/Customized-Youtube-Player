{
  "name": "example-create-react-app-express",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd client && yarn install && yarn add react-router-dom && yarn start",
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "mongoose": "^4.13.6",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  },
  "engines": {
    "node": "v11.8.0"
  }
}

